{
  "name": "android-tools-refactor",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
